# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.references

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Cache
.mypy_cache/
.pytest_cache/
.ruff_cache/

# State (user-specific)
.contents-hub/state.db*
.llm-wiki/state.db*

# Spec backups
*.backup-*

# Node (if any)
node_modules/

# contents-hub runtime state (keep bundled plugins under .contents-hub/plugins/)
.contents-hub/state.db*
.contents-hub/*.db
.contents-hub/*.log
.contents-hub/scheduled_tasks.lock

# legacy llm-wiki runtime state (compatibility fallback)
.llm-wiki/state.db*
.llm-wiki/*.db
.llm-wiki/*.log
.llm-wiki/scheduled_tasks.lock
.claude/scheduled_tasks.lock

# Vault content — promoted sources / generated digests are user data, not code
sources/
digests/
.gstack/

# QA evidence artifacts
.qa-reports/
